spring.application.name=inclass-spring-security

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# DataSource
# spring.datasource.url=jdbc:mysql://10.0.3.241:3306/spring_security_inclass?useSSL=false&serverTimezone=UTC
# JDBC URL 수정 : allow PublicKeyRetrieval=true 옵션 추가
spring.datasource.url=jdbc:mysql://10.0.3.203:3306/spring_security_inclass?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=cherish
spring.datasource.password=51228
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT ??
jwt.issuer=devrary
jwt.secret_key=0aa067d8-9355-4c41-adb3-b18307078da4

########## dev only ########################
# Spring DevTools LiveReload
spring.devtools.livereload.enabled=true

# Thymeleaf cache disable
spring.thymeleaf.cache=false

# Logging level to debug
logging.level.org.springframework=DEBUG

# Spring Session JDBC Store
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
############################################
